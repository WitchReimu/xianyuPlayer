file(GLOB source_file "*.cpp" "*.h")
add_library(player_mode ${source_file})
target_include_directories(player_mode PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
target_link_libraries(player_mode PRIVATE oboe::oboe)
target_link_libraries(player_mode PUBLIC FFMpeg_mode)
